# 本目录协作说明

## 项目目标

本目录用于持续维护可复制到其他项目中的 Codex/Agent 指令模板：

- `AGENTS.md.example`：英文模板。
- `AGENTS_ZH.md.example`：中文模板。
- `AGENTS.md`：本目录的实际协作规则，不是待复制的模板。

## 维护规则

1. 本次任务若未明确要求修改示例文件，只修改 `AGENTS.md`，不要顺手改动任何 `*.example` 文件。
2. 修改任一示例文件时，必须同步更新另一个示例文件，确保两者的章节、规则和约束覆盖范围一致。
3. 两个示例文件以内容语义一致为准，不要求逐字翻译；新增、删除或放宽一条规则时，另一种语言必须有对应变化。
4. 修改示例文件前，先通读两个版本并对照章节结构；修改后检查标题、列表、代码块和关键限定词是否同步。
5. 示例文件应保持通用，只描述可迁移的 Agent 工作规范；仅适用于本目录的背景、路径或临时决定写在本文件，不要混入示例。
6. 用户明确限定本次范围时，以用户限定为准；不要为了“顺便同步”扩大本次修改范围，但应在交付说明中指出待同步事项。

## 文档要求

- 面向中文用户沟通时使用简体中文；必要的技术名词、命令和文件名保留原文。
- 使用 UTF-8 保存 Markdown 文件，避免中文出现编码问题。
- 规则应优先写成清晰、可执行、可验证的要求；避免重复、含糊或无法判断是否满足的表述。
- 保持最小必要范围，不引入与当前任务无关的流程、工具或依赖。

## 开发环境

- 操作系统为 Windows，命令优先使用 PowerShell。

## 完成交付前

- 检查实际修改的文件，确认没有超出用户要求的范围。
- 对文档变更进行适当的文本或结构检查，并如实说明实际执行的检查及结果。
- 说明是否同步修改了两个示例文件；如果没有，明确说明原因和后续待办。
